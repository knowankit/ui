# Usage
```jsx
  <Button {...props}>
    Button Text
  </Button>

  // For buttons with icons
  <IconButton
      type="primary / default / danger / dashed"
      icon="icons"
      position="left / right"
      value="Button Text"
  />
```
